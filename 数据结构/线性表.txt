线性表
	线性表是n>=0个数据元素a1、a2...an的有限序列，表中每个元素，除首元素和尾元素
外，有且仅有一个直接前驱和直接后继。表中的每个元素个数定义为线性表的长度

		先行表记为(a1,a2,...,an)
		
操作
	随机存取：
		存取下表为k的节点，返回该节点的元素值
	插入：
		在下表为k的节点前（或后）插入一个新节点
	删除：
		删除小标为k的节点
	查找：
		寻找具有特定域值的节点
	归并、分拆、复制、计数、排序	